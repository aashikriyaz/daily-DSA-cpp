Count frequency of each element in the array
//we can use int instead of void 
//int is a return type ...void is like acting (printing)

#include <bits/stdc++.h>
using namespace std;

int Frequency(int arr[], int n)   // changed void → int
{
    unordered_map<int, int> map;

    for (int i = 0; i < n; i++)
        map[arr[i]]++;

    // Print frequencies
    for (auto x : map)
        cout << x.first << " " << x.second << endl;

    return 0;  // we must return an integer because function type is int
}

int main() {
    int arr[] = {10, 5, 10, 15, 10, 5};
    int n = sizeof(arr) / sizeof(arr[0]);
    Frequency(arr, n);   // same function call
    return 0;
}


